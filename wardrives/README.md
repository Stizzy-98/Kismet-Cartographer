# wardrives

Put your Kismet capture files (`*.kismet`) here, then run `./ingest` from the project folder.
You can install first and add files later; run `./ingest` again whenever you add more.

Everything in this folder except this file is ignored by git, so your captures (which contain GPS positions and other
people's network details) are never committed by accident.
